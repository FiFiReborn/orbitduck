# OrbitDuck Core Configuration
# ----------------------------

# Set to true to use mocked scans (for safe/offline testing)
MOCK_MODE = true

# Default targets if allowlist.txt is missing
DEFAULT_ALLOWLIST = example.com, google.com, github.com

# Optional SpiderFoot base URL (only needed for real mode)
SPIDERFOOT_BASE_URL = http://localhost:5001

# Optional Shodan API key
SHODAN_API_KEY = 

# Minimum seconds between scans (rate control)
SCAN_INTERVAL = 5
